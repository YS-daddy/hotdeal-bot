name: hotdeal-bot

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: install
        run: pip install requests beautifulsoup4

      - name: run bot
        run: python bot.py
